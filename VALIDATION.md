# V0.1 验收记录

## 策略编辑器扩展验收（2026-09-20）

- 新增规则编辑器与 Python 编辑器，保留内置策略说明和参数入口。
- 自动测试扩展到 30 项：规则与 Python 示例信号一致、历史前缀一致性、斜率/偏移/连续条件、卖出优先、参数边界、Python 语法及行号错误、输出契约、死循环超时、未来数据检查、版本不可覆盖、历史快照恢复，以及外部浏览器来源拒绝。
- Chromium 实测：规则策略修改后保存 V1/V2、校验并回测；Python DIF 斜率示例直接载入、校验、保存并回测。
- Python `1 / 0` 错误显示“第 2 行：ZeroDivisionError”；从历史回测恢复后原源码重新出现。
- 从自定义历史切回内置 MA 后仍可回测；390px 页面无整页横向溢出。
- 编辑器验收使用 `output/editor-validation.duckdb` 独立测试库与 8001/5174 端口，未向用户研究数据库写入测试策略。截图为 `output/playwright/rules-editor.png`、`output/playwright/python-editor.png`。
- Python 为可信本机代码执行，独立进程与 15 秒超时不是安全沙箱；历史前缀只作抽查。规则编辑器目前支持一层 AND/OR、行情指标、斜率、历史偏移和连续条件，不支持真实持仓状态条件或嵌套分组。

验收日期：2026-09-20（Asia/Shanghai）。环境：macOS arm64，Python 3.12.14，Node.js 22.22.0。

## 自动检查

- `./scripts/check.sh`：通过。
- Ruff：通过。
- Pytest：20 passed；有 2 条来自 Starlette/httpx/anyio 的上游弃用提示，不影响执行。
- TypeScript 与 Vite 生产构建：通过。
- `pip check`：无依赖冲突。
- 启动脚本已实际拉起本地前后端；`/api/health`、`/api/config`、前端代理 `/docs` 返回成功。

## 真实行情闭环

股票 `000938`，请求 2021-09-19 至 2026-09-19，腾讯源，前复权。实际保存 1,211 根日 K，2021-09-22 至 2026-09-18。

- 下载 → DuckDB → 行情加载 → MA 回测：31 笔完整交易。
- MACD 回测：46 笔完整交易。
- 历史记录恢复后仍使用保存的参数、行情和结果快照。
- 使用临时数据库验证真实增量：首次请求 2025-01-01 至 2025-01-10，写入 7 根；再更新到 2025-01-15，仅请求 2025-01-11 起的数据，追加 3 根，并完成复权锚点校验。
- 更新无新交易日的区间返回 0 行及明确提示。

东方财富/AKShare 接口已实现，但当前网络环境中的请求出现 ProxyError；其连接未作为本次真实数据验收的成功来源。腾讯是用户可明确选择的独立备用来源。腾讯 OHLCV 通道不返回成交额与换手率，这两个字段保存为空，不估算填充。

## 浏览器交互

使用真实 Chromium 和 Playwright CLI 验证：

- 下载历史、运行回测、MA/MACD 切换和参数非法提示。
- K 线、成交量、MACD 共享时间轴；权益和回撤曲线正常。
- 第 17 笔交易高亮，图表定位到持仓前后各 10 根 K 线，页面滚回图表。
- 持仓区间透明覆盖层可见，显示本笔净收益，不阻挡 K 线交互。
- 点击 BUY 箭头显示成交日、成交价、股数、策略、原因及信号日。
- 定位后滚轮缩放、拖动均可用，区间覆盖层随图同步。
- 历史回测记录分别恢复 31 笔 MA 和 46 笔 MACD 交易。
- 390px 宽度页面无整页横向溢出，宽交易表在局部容器滚动。
- 最终新浏览器会话无控制台错误；参数验证场景的 HTTP 422 是预期行为。

本地截图位于 `output/playwright/acceptance-chart.png`、`output/playwright/mobile.png`（不提交 Git）。

## 已声明的 V0.1 边界

未实现涨跌停封单、分红送转股数账本、历史费率自动切换、完整交易所日历。使用复权价格撮合是研究近似；交易规则、费用和统计口径详见 README。测试结果验证软件行为，不代表策略收益预测。

## 实验记录管理（2026-09-20）

- 后端测试共 36 项通过；新增验证收藏持久保存且快照不变、旧数据库兼容、UTF-8 快照大小与实际文件大小、删除子表并保留行情和策略、事务失败完整回滚、106 条记录分页、空选择和数量上限校验、来源保护及收藏删除保护。
- 在独立数据库 `output/history-validation-20260920.duckdb` 创建 4 条模拟实验，浏览器验证收藏/筛选、恢复历史、单条删除取消/确认、删除当前实验后清空结果、批量选择跳过收藏及成功删除其余 2 条。
- 删除测试未操作用户的研究数据库。空间显示明确区分实际数据库文件大小和未压缩快照字节数，不承诺删除后文件立即缩小。
- 界面截图：`output/playwright/history-manager.png`、`output/playwright/history-mobile.png`。
- 原始研究数据库启动前后核对：10 条实验的完整快照 SHA-256 全部一致，3,633 条共享行情保留；新管理接口正常返回。浏览器控制台无错误。


## 000938 波段启动研究（2026-09-21）

- 新增独立可编辑策略文件及说明，未修改回测引擎。完整后端测试53项通过；其中17项验证暖机、契约、逐个历史前缀一致性、突破基准、冷却、连续破位计数、ATR跟随线只升不降、无固定持仓上限和参数校验。
- 使用已有000938腾讯前复权真实缓存，2021-09-22至2026-09-18共1211根。首版趋势确认方案收益-12.43%保留；第二版小平台突破方案收益+63.79%，最大回撤-29.98%，21笔完整交易。设计参考过同一历史，因此均属探索性结果，不称样本外验证。
- 第二版全量策略通过真实本地API校验、保存、回测和快照读取，收益与离线计算一致。新增1条策略版本和1条实验，保留原有策略。
- 详细买点事后指标、全部交易/失败例、分段和参数敏感性见 examples/000938_swing/README.md；可用同目录 evaluate.py 从本地缓存只读复现。

## 自选股与自动增量更新（2026-09-21）

- 完整后端测试 66 项通过，代码检查与前端生产构建通过。新增 13 项测试覆盖旧行情自动识别、多股票/多数据源隔离、单只及全部增量、自动设置和检查状态跨重启保存、北京时间更新日期、模拟行情跳过、单只失败隔离与重试、复权变化保护、并发互斥和调度启停。
- 真实页面识别 000938、002396 共 3 份已保存行情。后台启动补查为 002396 腾讯前复权新增 2026-09-21 的 1 根日 K；000938 腾讯版本已覆盖该日，未重复下载。东方财富网络失败明确显示在对应版本，原行情保留。
- 浏览器验证搜索、股票切换并保留 fast_ma=7 参数、单只增量更新、自动更新开关保存及刷新后恢复、侧栏收起状态跨页面刷新保存。验收结束恢复自动更新为开启。
- 1440px 桌面和 390px 窄屏页面均无整页横向溢出；侧栏可展开或收起，图表随可用宽度调整。控制台无错误。
- 原有 7 条实验的完整 API 快照 SHA-256 核对全部一致，验收未新增或删除实验。
- 截图：`output/playwright/watchlist-desktop.png`、`output/playwright/watchlist-mobile-collapsed.png`、`output/playwright/watchlist-mobile-expanded.png`。

## 自选股简洁列表（2026-09-21）

- 默认只展示股票名称与代码，点击展开单只详情；收起侧栏再展开、刷新页面均恢复简洁列表。浏览器验证名称搜索、展开切换、股票切换及策略参数保留。
- 紫光股份（000938）、星网锐捷（002396）名称通过腾讯报价接口补齐并本地保存。新增测试验证名称解析、请求失败退避、写入行情不覆盖名称；完整 69 项测试、代码检查和前端构建通过。
- 界面截图：`output/playwright/watchlist-compact.png`。

## 默认选择最新行情来源（2026-09-21）

- 页面首次加载和侧栏切换股票统一使用来源选择规则：优先真实行情，保留当前复权方式（如存在该版本），比较最后行情日期；日期相同时优先更新正常的来源，再以当前来源作为平局选择。
- 浏览器验证 000938 首次打开自动使用腾讯 2026-09-21 的 1,212 根日 K；手动选择东方财富后，收起/展开详情不会覆盖选择；切换到其他股票再返回时重新选择最新来源。
- 7 个选择边界检查通过，涵盖日期排序、同日更新失败、模拟数据、复权方式、空缓存和空列表；前端类型检查、生产构建通过。
- 截图：`output/playwright/watchlist-latest-source.png`。
